/*Make the class including the variable name and value.
  We make this class easier to change the values of variables,
  and to facilitate the process and dealing with names*/

public class Constants {
    static int LEFT = 0;
    static int RIGHT = 1;
    static int initialMissionaryOnLeft = 3;
    static int initialCannibalOnLeft = 3;
    static int boatCapacity = 2;
}
